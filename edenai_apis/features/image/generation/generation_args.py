from typing import Dict

def generation_arguments() -> Dict:
    return {
        "text": "A huge red ballon flying outside the city.",
        "num_images":2,
        "width" : 512,
        "height" : 512,
        }
